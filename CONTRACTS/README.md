# Contracts

Pinned shared surfaces — DB schema, API types, module boundaries. Written by the `plan` verb; workers build against these, never against each other's context. Changing a contract requires a DECISIONS.md entry.

_Empty until Define._
