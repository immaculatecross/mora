# Control

Steering queue: the Owner appends, the Foreman drains between PRs (D-014). Entries: `- [ ] <timestamp> — <instruction>`; the Foreman checks them off with a one-line response. Emergency stop is an Owner command, not a queue entry.

_Empty._
